{
  "name": "fiello-model-transport-proxy",
  "displayName": "Fiello Model Transport Proxy",
  "description": "Routes custom VS Code model requests through a local HTTP/1.1 proxy to avoid transient HTTP/2 transport failures.",
  "version": "0.2.2",
  "publisher": "serhiiyarovyi",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/SergeyYarovoy/Custom-Model-HTTP-1.1-Proxy.git"
  },
  "homepage": "https://github.com/SergeyYarovoy/Custom-Model-HTTP-1.1-Proxy#readme",
  "bugs": {
    "url": "https://github.com/SergeyYarovoy/Custom-Model-HTTP-1.1-Proxy/issues"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "keywords": [
    "custom model",
    "http1",
    "http2",
    "proxy",
    "language model",
    "openai"
  ],
  "pricing": "Free",
  "preview": true,
  "activationEvents": [
    "onStartupFinished"
  ],
  "extensionKind": [
    "ui"
  ],
  "main": "./extension.js",
  "files": [
    "extension.js",
    "proxy-core.js",
    "README.md",
    "CHANGELOG.md",
    "LICENSE"
  ],
  "scripts": {
    "test": "node --test",
    "package": "npx --yes @vscode/vsce package --allow-missing-repository",
    "publish": "npx --yes @vscode/vsce publish --allow-missing-repository"
  },
  "contributes": {
    "commands": [
      {
        "command": "customModelHttp1Proxy.showStatus",
        "title": "Fiello Model Transport Proxy: Show Status"
      },
      {
        "command": "customModelHttp1Proxy.configure",
        "title": "Fiello Model Transport Proxy: Configure Upstream"
      },
      {
        "command": "customModelHttp1Proxy.restart",
        "title": "Fiello Model Transport Proxy: Restart"
      }
    ],
    "configuration": {
      "title": "Fiello Model Transport Proxy",
      "properties": {
        "customModelHttp1Proxy.upstreamUrl": {
          "type": "string",
          "default": "",
          "scope": "application",
          "markdownDescription": "Full HTTPS endpoint URL that receives proxied custom model requests. Credentials remain in the custom model configuration and are forwarded as request headers."
        },
        "customModelHttp1Proxy.maxProviderRetries": {
          "type": "integer",
          "default": 5,
          "minimum": 0,
          "maximum": 30,
          "scope": "application",
          "markdownDescription": "Maximum retries after a transient provider SSE error. Delays increase progressively and honor the provider's `Retry-After` header."
        }
      }
    }
  },
  "capabilities": {
    "untrustedWorkspaces": {
      "supported": true
    },
    "virtualWorkspaces": {
      "supported": true
    }
  }
}